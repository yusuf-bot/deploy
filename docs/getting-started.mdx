---
title: Getting Started
description: Install deploy and ship your first app in 3 minutes.
---

# Getting Started

`deploy` is a single-binary personal PaaS. One command installs it, one command deploys your app.

## Prerequisites

- A Linux server (x86_64 or ARM64)
- Docker Engine 20.0+ installed and running
- Root access (for daemon setup)

## Install

```bash
curl -fsSL https://deploy.openexplorer.xyz/install.sh | sh
```

This downloads the binary, checks Docker, installs Caddy, and runs the setup wizard.

## Initialize

```bash
sudo deploy init
```

The init wizard will:
1. Create `~/.deploy/` directory with SQLite database
2. Generate master encryption key for secrets
3. Download Caddy for automatic SSL
4. Optionally install systemd service for auto-start
5. **Interactive wizard** — prompts for auto-start, shows summary

> **Non-interactive environments?** The wizard auto-skips in CI/scripts. Run `deploy init --no-systemd` to skip systemd setup explicitly.

## Deploy Your First App

```bash
# Create a simple Go app
mkdir myapp && cd myapp
cat > main.go << 'EOF'
package main

import (
    "fmt"
    "net/http"
)

func main() {
    http.HandleFunc("/health", func(w http.ResponseWriter, r *http.Request) {
        w.WriteHeader(http.StatusOK)
        fmt.Fprintf(w, "ok")
    })
    http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
        fmt.Fprintf(w, "Hello from deploy!")
    })
    http.ListenAndServe(":8080", nil)
}
EOF

go mod init myapp

# Scaffold deploy.yml + Dockerfile
deploy scaffold


# Deploy!
deploy up myapp
```

## Next Steps

- [Add a domain](domains-dns) with automatic SSL
- [Configure deploy.yml](deploy-yml) for your app
- [Set up secrets](secrets) for environment variables
- [Use dev mode](development) for local development
